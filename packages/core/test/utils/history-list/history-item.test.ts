import { describe, it } from 'vitest';

describe('history-item', () => {
  it('historyItem.clone', () => {
    //   const prev = new HistoryItem({ a: 1 });
    //   const mid = new HistoryItem({ b: 2 });
    //   const next = new HistoryItem({ c: 3 });
    //   prev._next = mid;
    //   mid._prev = prev;
    //   mid._next = next;
    //   next._prev = mid;
    //   expect(clone(mid)).toEqual(mid);
  });
});
