import { IApiEnforceableController } from '../../../api';

/**
 * @description 可以调用force的控制器接口
 * @export
 * @interface IEnforceableController
 * @extends {IApiEnforceableController}
 */
export interface IEnforceableController extends IApiEnforceableController {}
